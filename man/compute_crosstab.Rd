% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{compute_crosstab}
\alias{compute_crosstab}
\title{Compute a cross-tabulation of two categorical variables}
\usage{
compute_crosstab(df, row_var, col_var, strat_var = NULL)
}
\arguments{
\item{df}{A \code{data.frame} or tibble.}

\item{row_var}{Character string. Name of the row variable (e.g. \code{"SEX"}).}

\item{col_var}{Character string. Name of the column variable (e.g. \code{"RACE"}).}

\item{strat_var}{Character string or \code{NULL}. Optional stratification
variable (e.g. \code{"TRT01P"}).  Pass \code{NULL} or \code{""} for an unstratified
table.}
}
\value{
A \code{data.frame} in wide format:
\itemize{
\item Column 1 (or 2 if stratified): \code{row_var} levels plus a \code{"Total"} row.
\item Middle columns: one column per \code{col_var} level.
\item Last column: \code{Total} (row sums).
\item If \code{strat_var} is given, a leading \code{Stratum} column identifies
each stratum.  A grand-total block across all strata is \strong{not}
appended automatically — compute the unstratified table for that.
}
}
\description{
Produces a wide-format contingency table of \code{row_var} (rows) by \code{col_var}
(columns), including row and column totals.  When a \code{strat_var} is supplied
the table is computed separately for each level of the stratification
variable and the results are stacked with a leading \code{Stratum} column.
}
\details{
Missing values in any of the three variables are displayed as \code{"(Missing)"}
rather than being silently dropped, so analysts can spot incomplete records.
}
\examples{
df <- data.frame(
  SEX  = c("M","F","M","F","M","F"),
  RACE = c("White","White","Black","Asian","Black","White"),
  TRT  = c("A","A","B","B","A","B")
)
compute_crosstab(df, "SEX", "RACE")
compute_crosstab(df, "SEX", "RACE", strat_var = "TRT")

}
