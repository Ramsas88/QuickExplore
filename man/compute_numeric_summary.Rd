% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{compute_numeric_summary}
\alias{compute_numeric_summary}
\title{Compute summary statistics for numeric variables}
\usage{
compute_numeric_summary(df, vars, group_var = NULL)
}
\arguments{
\item{df}{A \code{data.frame} or tibble.}

\item{vars}{Character vector of variable names to summarise.}

\item{group_var}{Optional character string naming a grouping variable.
Pass \code{NULL} (default) for no grouping.}
}
\value{
A \code{data.frame} (one row per variable, or per variable × group level)
or \code{NULL} if there are no numeric variables in \code{vars}.
}
\description{
Returns a tidy data frame with N, mean, median, standard deviation,
minimum, and maximum for each numeric variable in \code{vars}.
}
\examples{
df <- data.frame(x = rnorm(100), y = runif(100), g = rep(c("A", "B"), 50))
compute_numeric_summary(df, c("x", "y"))
compute_numeric_summary(df, c("x", "y"), group_var = "g")

}
